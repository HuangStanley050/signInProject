<h1>Logins#secret</h1>
<h2>Secret Page for entering bsb</h2>
<%= form_with(url: "/logins/create", method: "post") do %>
  <%= label_tag(:bsb, "Enter your bsb details") %>
  <%= text_field_tag(:secret) %> <!--- :secret is the names for the parameter --->
  <%= submit_tag("Submit") %>
<% end %>
<%= link_to "logout", destroy_user_session_path, method: :delete %>
